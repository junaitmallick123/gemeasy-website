(()=>{const reduce=window.matchMedia('(prefers-reduced-motion: reduce)').matches;if(reduce)return;document.documentElement.classList.add('motion-ready');const header=document.querySelector('.site-header');const onScroll=()=>header&&header.classList.toggle('motion-scrolled',window.scrollY>18);onScroll();addEventListener('scroll',onScroll,{passive:true});const revealSelectors=['main section > .container','main .newsletter','main .knowledge-cta','main .tender-notice','main .tender-help-panel'];const reveal=[...document.querySelectorAll(revealSelectors.join(','))];reveal.forEach((el,i)=>{el.classList.add('motion-reveal');el.style.setProperty('--motion-delay',(i%3)*55+'ms')});const staggerSelectors=['.service-grid','.stats-grid','.process-grid','.review-track','.capability-grid','.values-grid','.news-feed-grid','.tender-type-grid','.tender-process-grid','.guide-stack','.contact-points'];const stagger=[...document.querySelectorAll(staggerSelectors.join(','))];stagger.forEach(group=>{group.classList.add('motion-stagger');[...group.children].forEach((child,i)=>child.style.setProperty('--motion-index',Math.min(i,8)))});const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');observer.unobserve(entry.target)}}),{threshold:.12,rootMargin:'0px 0px -7% 0px'});[...reveal,...stagger].forEach(el=>observer.observe(el));document.querySelectorAll('.hero-visual,.tender-status-card,.about-team-photo').forEach(el=>el.classList.add('is-floating'));if(matchMedia('(pointer:fine)').matches){const reactive=document.querySelector('.hero-visual,.tender-status-card');if(reactive){let frame;const move=e=>{cancelAnimationFrame(frame);frame=requestAnimationFrame(()=>{const r=reactive.getBoundingClientRect(),x=(e.clientX-r.left-r.width/2)/r.width,y=(e.clientY-r.top-r.height/2)/r.height;reactive.style.setProperty('--motion-x',(x*7)+'px');reactive.style.setProperty('--motion-y',(y*5)+'px')})};const reset=()=>{reactive.style.setProperty('--motion-x','0px');reactive.style.setProperty('--motion-y','0px')};reactive.addEventListener('pointermove',move,{passive:true});reactive.addEventListener('pointerleave',reset)}}setTimeout(()=>document.querySelectorAll('.motion-reveal:not(.is-visible),.motion-stagger:not(.is-visible)').forEach(el=>el.classList.add('is-visible')),1800)})();